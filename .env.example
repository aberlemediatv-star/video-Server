# Für docker compose (optional)
VOD_PUBLIC_BASE=http://localhost:8080/vod

# Auth — OIDC > ADMIN_API_KEY > offen (nur Dev!)
ADMIN_API_KEY=
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_AUDIENCE=

ADMIN_DEV_ORIGIN=http://localhost:5174
PLAYER_DEV_ORIGIN=http://localhost:5173

MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345
DATABASE_URL=postgres://app:app@127.0.0.1:5432/video_server

# Observability (nur wenn `--profile observability` aktiv)
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# Für Vite (optional; leer = Proxy /api)
# VITE_API_URL=http://localhost:3000
